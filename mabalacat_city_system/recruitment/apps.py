"""App configuration for recruitment module."""

from django.apps import AppConfig


class RecruitmentConfig(AppConfig):
    """Django AppConfig for recruitment application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "recruitment"
